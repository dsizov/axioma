{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Authors creation</h1>

    <form action="{{ path('authors_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('authors') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
