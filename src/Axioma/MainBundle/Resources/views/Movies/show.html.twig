{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Movies</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ entity.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Quality</th>
                <td>{{ entity.quality }}</td>
            </tr>
            <tr>
                <th>Actor</th>
                <td>
                {% for actor in entity.actor %}
                    {{ actor.name }}{% if not loop.last %},{% endif %}
                {% endfor %}
                </td>
            </tr>
            <tr>
                <th>Tag</th>
                <td>
                {% for tag in entity.tag %}
                    {{ tag.name }}{% if not loop.last %},{% endif %}
                {% endfor %}
                </td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('movies') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('movies_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('movies_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
